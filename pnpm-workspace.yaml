# No dependency here needs to run install scripts (core-js, a transitive jspdf
# dep, only prints a funding notice). Denying it explicitly makes that a recorded
# decision rather than an ERR_PNPM_IGNORED_BUILDS failure, and keeps one
# supply-chain foothold closed: a compromised release cannot execute on install.
allowBuilds:
  core-js: false
